# Default backend — free key from https://aistudio.google.com/apikey
# (free tier is ~20 requests/day = about one agent run per day)
GOOGLE_API_KEY=

# Optional alternative — free key from https://console.groq.com/keys
# (no credit card; ~14,400 requests/day; use make_llm("groq"))
GROQ_API_KEY=
