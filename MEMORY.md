# Agent memory

Lessons appended by the agents via the `remember` tool; read into the lead's
planning prompt at the start of every run. Delete lines freely.
- To compare LCDM vs LCDM+0.20eV neutrinos, compute both spectra at z=0, then plot with LCDM as ratio reference and eBOSS DR14 data.
- plot_power_spectra uses the first file in spectrum_files as the ratio reference (reference_index 0).
